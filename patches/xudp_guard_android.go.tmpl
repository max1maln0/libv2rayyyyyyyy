//go:build android
// +build android

// Этот файл гарантирует, что на ANDROID мы не инициируем XUDP и не трогаем basekey.
// Даже если в коде остались вызовы xudpMaybeInit() — под android init() в этом файле
// сработает вместо любой другой инициализации (порядок обеспечивают build tags).

package __PKG__

func init() {
    // Ничего не делаем. Любая XUDP-инициализация на Android — запрещена.
    // Если где-то есть парсер basekey — он должен быть «мягким» (наши патчи это делают).
}
